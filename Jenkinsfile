pipeline {
   agent any

   stages {
      stage('buildcode) {
         steps {
            echo 'build'
         }
      }
      stage('deployment') {
         steps {
            echo 'deployment'
         }
      }
      stage('test') {
         steps {
            echo 'test'
         }
      }
   }
}
