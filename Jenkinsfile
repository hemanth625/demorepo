pipeline {
   agent any

   stages {
      stage('buildcode) {
         steps {
            echo 'build'
         }
      }
      stage('deployment') {
         steps {
            echo 'deploy'
         }
      }
      stage('test') {
         steps {
            echo 'test'
         }
      }
   }
}
