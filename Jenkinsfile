pipeline {
   agent any

   stages {
      stage('build') {
         steps {
            echo 'build'
         }
      }
      stage('deployment') {
         steps {
            echo 'deploy'
         }
      }
      stage('test') {
         steps {
            echo 'test'
         }
      }
   }
}
